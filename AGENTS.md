# AGENTS.md

Agent guidance for this repository lives in [`CLAUDE.md`](./CLAUDE.md) — it is
tool-agnostic: build, test and gate commands, the four rules that govern every
change, and what a test can and cannot prove about a trust store. Read that
file, and `src/truststores.ads` for the caller's contract.
